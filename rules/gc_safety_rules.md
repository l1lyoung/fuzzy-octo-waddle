# Unreal Engine C++ Memory & GC Safety Rules

1. Raw UObject Pointers must use UPROPERTY().
2. Dynamic Multicast Delegates for Blueprint Events.
3. Include generated.h strictly as the last include header.
